import 'package:flutter/material.dart';

class TitlesNavBar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: TabBar(tabs: []),
    );
  }
}
